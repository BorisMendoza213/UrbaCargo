import { Pressable, StyleSheet, Text, View } from 'react-native';
import { router } from 'expo-router';
import { Icon } from '@/components/Icon';
import { colors } from '@/src/theme';

export default function Perfil() { return <View style={styles.page}><Text style={styles.kicker}>TU CUENTA</Text><Text style={styles.title}>Perfil</Text><View style={styles.profile}><View style={styles.avatar}><Text style={styles.avatarText}>CM</Text></View><View><Text style={styles.name}>Camila Méndez</Text><Text style={styles.email}>camila@ejemplo.com</Text></View></View>{[['person-outline','Datos personales'],['card-outline','Métodos de pago'],['leaf-outline','Mi impacto ambiental'],['help-circle-outline','Centro de ayuda']].map(([icon, label]) => <Pressable style={styles.item} key={label}><Icon name={icon as any} size={21} color={colors.moss} /><Text style={styles.itemText}>{label}</Text><Icon name="chevron-forward" size={17} color={colors.muted} /></Pressable>)}<Pressable style={styles.logout} onPress={() => router.replace('/login')}><Icon name="log-out-outline" size={19} color={colors.red} /><Text style={styles.logoutText}>Cerrar sesión</Text></Pressable></View> }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.cream, padding: 24, paddingTop: 64 }, kicker: { color: colors.muted, fontSize: 10, fontWeight: '800', letterSpacing: 1.3 }, title: { color: colors.ink, fontSize: 29, fontWeight: '800', marginTop: 6 }, profile: { backgroundColor: colors.mint, borderRadius: 18, padding: 20, marginTop: 28, flexDirection: 'row', alignItems: 'center', gap: 14 }, avatar: { width: 55, height: 55, borderRadius: 18, backgroundColor: colors.lime, alignItems: 'center', justifyContent: 'center' }, avatarText: { color: colors.ink, fontWeight: '800', fontSize: 17 }, name: { color: colors.ink, fontWeight: '800', fontSize: 16 }, email: { color: colors.muted, fontSize: 12, marginTop: 5 }, item: { height: 64, borderBottomWidth: 1, borderBottomColor: colors.line, flexDirection: 'row', alignItems: 'center', gap: 14 }, itemText: { color: colors.ink, flex: 1, fontSize: 14, fontWeight: '600' }, logout: { flexDirection: 'row', gap: 12, alignItems: 'center', marginTop: 32 }, logoutText: { color: colors.red, fontWeight: '700' } });
